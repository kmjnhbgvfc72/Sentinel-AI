export default function ThreatFeed({ threats }) { return <section className="panel feed"><div className="panel-title"><h2>Latest threat feed</h2><span>{threats.length} records</span></div>{threats.map((item) => <article className="feed-item" key={item.id}><div><span className={`severity ${item.severity}`}>{item.severity}</span><h3>{item.name}</h3><p>{item.description}</p><small>{item.source} · {new Date(item.created_at).toLocaleString()}</small></div><strong className="risk">{item.risk_score}<small>/100</small></strong></article>)}</section>; }
