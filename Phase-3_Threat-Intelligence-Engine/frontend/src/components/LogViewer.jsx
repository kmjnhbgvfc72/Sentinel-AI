export default function LogViewer({ logs }) { return <section className="panel"><div className="panel-title"><h2>Security logs</h2><span>Sanitized defensive events</span></div>{logs.map((item) => <article className="log-row" key={item.id}><div><strong>{item.event_type.replaceAll("_", " ")}</strong><small>{item.user_id} · {item.ip_address || "No IP"}</small></div><span className={`severity ${item.risk_level}`}>{item.risk_level}</span></article>)}</section>; }
