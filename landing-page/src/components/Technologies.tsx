import { Code2 } from 'lucide-react'
import { technologies } from '../data/project'

export function Technologies() { return <section className="section technologies" aria-labelledby="technology-title"><div className="container technologies__layout"><div className="section-heading"><span className="kicker">ENGINEERING STACK</span><h2 id="technology-title">Built with a practical,<br/><em>open technology stack.</em></h2><p>Technologies shown here are verified from the project's manifests, requirements, deployment configuration, and documentation.</p><div className="technology-note"><Code2/><span><strong>Designed for inspection</strong><small>Versioned APIs, typed configuration, reproducible containers, and documented phase boundaries.</small></span></div></div><div className="technology-grid">{technologies.map(technology => <article key={technology.name}><span>{technology.group}</span><strong>{technology.name}</strong><small>{technology.detail}</small></article>)}</div></div></section> }
