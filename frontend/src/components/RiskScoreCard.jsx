export default function RiskScoreCard({ score = 0 }) {
  const safeScore = Math.max(0, Math.min(100, Number(score) || 0))
  return <article className="panel risk-panel"><div className="panel-heading"><div><span className="eyebrow">AI risk engine</span><h2>Organization risk score</h2></div><span className={`severity ${safeScore >= 70 ? 'severity-critical' : safeScore >= 40 ? 'severity-high' : 'severity-low'}`}>{safeScore >= 70 ? 'Elevated' : safeScore >= 40 ? 'Guarded' : 'Low'}</span></div><div className="risk-content"><div className="risk-ring" style={{ '--score': `${safeScore * 3.6}deg` }}><div><strong>{safeScore}</strong><span>/100</span></div></div><div className="risk-legend"><p><i className="critical-dot" />Critical exposure <strong>{Math.round(safeScore * .18)}</strong></p><p><i className="warning-dot" />Active warnings <strong>{Math.round(safeScore * .42)}</strong></p><p><i className="safe-dot" />Controls healthy <strong>{Math.max(0, 100 - safeScore)}%</strong></p></div></div></article>
}
