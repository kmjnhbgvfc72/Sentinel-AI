import { Bell, BrainCircuit, ChevronLeft, ChevronRight, FileChartColumn, GitBranch, LayoutDashboard, MonitorCog, Radar, Server, ShieldAlert, TriangleAlert, X } from "lucide-react";
import { NavLink } from "react-router-dom";

const links = [{ to: "/", label: "Dashboard", icon: LayoutDashboard }, { to: "/threats", label: "Threats", icon: TriangleAlert }, { to: "/intelligence", label: "Threat Intelligence", icon: Radar }, { to: "/ai-analysis", label: "AI Analysis", icon: BrainCircuit }, { to: "/attack-analysis", label: "Attack Analysis", icon: GitBranch }, { to: "/alerts", label: "Alerts", icon: Bell }, { to: "/assets", label: "Assets", icon: Server }, { to: "/reports", label: "Reports", icon: FileChartColumn }, { to: "/settings", label: "Settings", icon: MonitorCog }];

export default function Sidebar({ collapsed, setCollapsed, mobileOpen, setMobileOpen }) {
  return <><div className={`drawer-backdrop ${mobileOpen ? "visible" : ""}`} onClick={() => setMobileOpen(false)} aria-hidden="true"/><aside className={`sidebar ${collapsed ? "collapsed" : ""} ${mobileOpen ? "mobile-open" : ""}`} aria-label="Primary navigation"><div className="logo"><ShieldAlert/><div><strong>Sentinel AI</strong><span>SOC Platform</span></div><button className="icon-button mobile-close" onClick={() => setMobileOpen(false)} aria-label="Close navigation"><X/></button></div><nav>{links.map(({ to, label, icon: Icon }) => <NavLink key={to} to={to} end={to === "/"} onClick={() => setMobileOpen(false)}><Icon/><span>{label}</span></NavLink>)}</nav><button className="collapse-button" onClick={() => setCollapsed(!collapsed)} aria-label={collapsed ? "Expand sidebar" : "Collapse sidebar"}>{collapsed ? <ChevronRight/> : <><ChevronLeft/><span>Collapse navigation</span></>}</button></aside></>;
}
