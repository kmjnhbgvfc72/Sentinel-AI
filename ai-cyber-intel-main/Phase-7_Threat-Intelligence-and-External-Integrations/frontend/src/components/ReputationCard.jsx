import {ShieldCheck,ShieldAlert} from 'lucide-react'
export default function ReputationCard({result}){if(!result)return <div className="panel muted">Submit an indicator to inspect local defensive intelligence.</div>;const risky=result.score>=50;const Icon=risky?ShieldAlert:ShieldCheck;return <div className="panel"><div className="flex items-center justify-between"><Icon className={risky?'text-danger':'text-aqua'} size={30}/><span className="text-4xl font-bold">{result.score}</span></div><p className="eyebrow mt-5">{result.verdict}</p><h3 className="mt-2 break-all font-mono">{result.value}</h3><p className="muted mt-4">{result.details.note}</p><p className="mt-3 text-xs text-slate-500">Sources: {result.sources.join(', ')||'No matches'} · {result.cached?'Cached':'Fresh'}</p></div>}
