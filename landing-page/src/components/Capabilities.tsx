import { capabilities } from '../data/project'

export function Capabilities() { return <section className="section capabilities" id="features" aria-labelledby="capabilities-title"><div className="container"><div className="section-heading section-heading--center"><span className="kicker">PLATFORM CAPABILITIES</span><h2 id="capabilities-title">Built across the full<br/><em>security decision cycle.</em></h2><p>Each capability is grounded in an implemented project phase and connected through the central SOC architecture.</p></div><div className="capabilities__grid">{capabilities.map(({ title, text, icon: Icon, phase }) => <article className="capability-card" key={title}><div className="capability-card__top"><span><Icon/></span><small>{phase}</small></div><h3>{title}</h3><p>{text}</p><div className="capability-card__line"/></article>)}</div></div></section> }
