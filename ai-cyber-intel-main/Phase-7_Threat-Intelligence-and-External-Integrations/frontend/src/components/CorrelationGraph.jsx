import {ResponsiveContainer,BarChart,Bar,XAxis,YAxis,Tooltip,CartesianGrid} from 'recharts'
export default function CorrelationGraph({items=[]}){const data=Object.values(items.reduce((a,x)=>{a[x.source_phase]??={phase:x.source_phase,matches:0,score:0};a[x.source_phase].matches+=1;a[x.source_phase].score=Math.max(a[x.source_phase].score,x.score);return a},{}));return <div className="panel h-80"><h3 className="font-semibold">Cross-phase correlation</h3><p className="muted mb-5">Exact IOC matches and highest contextual score</p><ResponsiveContainer width="100%" height="75%"><BarChart data={data}><CartesianGrid stroke="#1d3651" vertical={false}/><XAxis dataKey="phase" stroke="#7790aa"/><YAxis stroke="#7790aa" domain={[0,100]}/><Tooltip contentStyle={{background:'#0c1929',border:'1px solid #1d3651'}}/><Bar dataKey="score" fill="#45d6c7" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div>}
