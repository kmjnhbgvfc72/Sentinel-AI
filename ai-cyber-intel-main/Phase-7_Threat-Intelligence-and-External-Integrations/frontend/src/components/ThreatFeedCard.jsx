import {Radio,TowerControl} from 'lucide-react'
export default function ThreatFeedCard({feed,status}){const healthy=status?.status==='healthy';return <article className="panel"><div className="flex items-start justify-between"><div className="rounded-xl bg-aqua/10 p-3 text-aqua"><Radio size={20}/></div><span className={`badge ${healthy?'bg-aqua/10 text-aqua':'bg-slate-700 text-slate-300'}`}>{status?.status||'never synced'}</span></div><h3 className="mt-5 font-semibold">{feed.name}</h3><p className="muted mt-1">{feed.format.toUpperCase()} · Reliability {feed.reliability}%</p><div className="mt-4 flex items-center gap-2 text-xs text-slate-500"><TowerControl size={14}/>{feed.enabled?'Active synchronization':'Disabled by default'}</div></article>}
