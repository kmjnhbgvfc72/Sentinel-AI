export default function ThreatTimeline({ threats }) { return <section className="panel"><div className="panel-title"><h2>Threat timeline</h2></div><ol className="timeline">{threats.map((item) => <li key={item.id}><time>{new Date(item.created_at).toLocaleString()}</time><strong>{item.name}</strong><span>{item.type} · {item.confidence_score}% confidence</span></li>)}</ol></section>; }
