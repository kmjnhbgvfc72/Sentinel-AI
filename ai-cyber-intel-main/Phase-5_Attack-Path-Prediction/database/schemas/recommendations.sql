CREATE TABLE IF NOT EXISTS recommendations (id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY, threat_id VARCHAR(128) NOT NULL, recommendation TEXT NOT NULL, priority VARCHAR(20) NOT NULL, status VARCHAR(30) NOT NULL DEFAULT 'open', created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP);
