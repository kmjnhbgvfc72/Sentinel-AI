import {Trash2} from 'lucide-react'
export default function IOCViewer({items=[],onDelete}){return <div className="panel table-wrap"><table className="data-table"><thead><tr><th>Indicator</th><th>Type</th><th>Threat</th><th>Confidence</th><th>Severity</th><th/></tr></thead><tbody>{items.map(i=><tr key={i.id}><td className="max-w-64 truncate font-mono text-aqua">{i.value}</td><td>{i.type}</td><td>{i.threat_type}</td><td>{i.confidence}%</td><td><span className={`badge ${i.severity==='critical'?'bg-danger/10 text-danger':'bg-amber/10 text-amber'}`}>{i.severity}</span></td><td><button aria-label={`Delete ${i.value}`} className="text-slate-500 hover:text-danger" onClick={()=>onDelete(i.id)}><Trash2 size={17}/></button></td></tr>)}</tbody></table>{!items.length&&<p className="muted py-8 text-center">No indicators match the current filter.</p>}</div>}
