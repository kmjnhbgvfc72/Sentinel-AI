# Frontend

React and Vite SOC console for the AI Cyber Threat Intelligence System.

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL` when the API is not available at `http://localhost:8000/api/v1`.
