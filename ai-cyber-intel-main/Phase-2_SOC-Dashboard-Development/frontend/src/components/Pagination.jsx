export default function Pagination({ meta, onPage }) { if (!meta || meta.pages <= 1) return null; return <nav className="pagination" aria-label="Pagination"><button className="button secondary small" disabled={meta.page <= 1} onClick={() => onPage(meta.page - 1)}>Previous</button><span>Page {meta.page} of {meta.pages} · {meta.total} records</span><button className="button secondary small" disabled={meta.page >= meta.pages} onClick={() => onPage(meta.page + 1)}>Next</button></nav>; }
