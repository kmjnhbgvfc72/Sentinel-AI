CREATE TABLE IF NOT EXISTS risk_scores (id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY, asset_id BIGINT NOT NULL, risk_score DOUBLE PRECISION NOT NULL CHECK(risk_score BETWEEN 0 AND 100), severity VARCHAR(20) NOT NULL);
