import { workflow } from '../data/project'

export function Workflow() { return <section className="section workflow" id="workflow" aria-labelledby="workflow-title"><div className="container"><div className="section-heading section-heading--center"><span className="kicker">SYSTEM WORKFLOW</span><h2 id="workflow-title">Evidence moves forward.<br/><em>Context compounds.</em></h2><p>The project connects collection, analysis, prediction, investigation, and response while retaining analyst visibility.</p></div><div className="workflow__track">{workflow.map(({ number, title, text, icon: Icon }) => <article className="workflow-step" key={number}><div className="workflow-step__head"><span>{number}</span><Icon/></div><h3>{title}</h3><p>{text}</p></article>)}</div></div></section> }
