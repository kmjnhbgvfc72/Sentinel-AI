CREATE TABLE IF NOT EXISTS attack_graph (id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY, node_type VARCHAR(50) NOT NULL, node_value VARCHAR(160) NOT NULL, relationship VARCHAR(100) NOT NULL, target_node VARCHAR(160) NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP);
