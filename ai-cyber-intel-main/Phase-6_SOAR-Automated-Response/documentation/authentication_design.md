# Authentication design
The API supports an environment-provided bearer token for administrative writes and role definitions for Admin, SOC Analyst, and Viewer. Replace this adapter with the organization’s OIDC provider before production.
