CREATE TABLE IF NOT EXISTS assets (id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY, asset_name VARCHAR(160) UNIQUE NOT NULL, asset_type VARCHAR(80) NOT NULL, criticality INTEGER NOT NULL CHECK(criticality BETWEEN 0 AND 100), risk_score DOUBLE PRECISION NOT NULL CHECK(risk_score BETWEEN 0 AND 100));
