import { CartesianGrid, Legend, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
export default function PredictionChart({ predictions, risks }) { const points = predictions.slice().reverse().map((item, index) => ({ event: item.event_id.slice(-10), confidence: item.confidence_score, risk: risks.slice().reverse()[index]?.risk_score || 0 })); return <section className="panel chart-panel"><h2>Prediction trend</h2><ResponsiveContainer width="100%" height={270}><LineChart data={points}><CartesianGrid stroke="#20394f"/><XAxis dataKey="event" stroke="#7890a7"/><YAxis domain={[0, 100]} stroke="#7890a7"/><Tooltip/><Legend/><Line dataKey="confidence" stroke="#22d3ee" strokeWidth={2}/><Line dataKey="risk" stroke="#fb7185" strokeWidth={2}/></LineChart></ResponsiveContainer></section>; }
