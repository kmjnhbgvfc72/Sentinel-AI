export default function AttackPathCard({ path }) { let nodes = []; try { nodes = JSON.parse(path.path); } catch { nodes = [path.source_node, path.destination_node]; } return <article className="path-card"><span className={`badge ${path.risk_level}`}>{path.risk_level}</span><h3>{nodes.join("  →  ")}</h3><strong>{path.risk_score}/100 risk</strong><small>{new Date(path.created_at).toLocaleString()}</small></article>; }
