export default function IOCViewer({ indicators }) { return <section className="panel"><div className="panel-title"><h2>Indicators of compromise</h2></div><div className="ioc-grid">{indicators.map((item) => <article key={item.id}><span>{item.indicator_type}</span><code>{item.value}</code><strong>{item.confidence_score}% confidence</strong><small>{item.threat_category} · {item.status}</small></article>)}</div></section>; }
