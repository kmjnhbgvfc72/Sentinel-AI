import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from "recharts";
const COLORS = { critical: "#fb7185", high: "#fb923c", medium: "#facc15", low: "#38bdf8" };
export default function ThreatStatistics({ statistics }) { const chart = Object.entries(statistics.by_severity || {}).map(([name, value]) => ({ name, value })); return <section className="panel"><div className="panel-title"><h2>Threat statistics</h2></div><div className="stat-row"><div><strong>{statistics.total_threats}</strong><span>Total</span></div><div><strong>{statistics.high_risk_threats}</strong><span>High risk</span></div><div><strong>{statistics.overall_risk_score}</strong><span>Risk score</span></div></div><ResponsiveContainer width="100%" height={210}><PieChart><Pie data={chart} dataKey="value" nameKey="name" innerRadius={50} outerRadius={78}>{chart.map((item) => <Cell key={item.name} fill={COLORS[item.name]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></section>; }
